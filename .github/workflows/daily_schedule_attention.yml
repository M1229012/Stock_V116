name: 每日更新處置股出關記錄

on:
  schedule:
    # 每天台灣時間下午 2:30 (UTC 06:30) 執行
    - cron: '30 6 * * *'
  workflow_dispatch: # 允許手動按按鈕執行

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install gspread google-auth requests yfinance pandas numpy

      - name: Create Service Key
        # 👇 這裡改用 GCP_SERVICE_KEY
        env:
          GCP_SERVICE_KEY: ${{ secrets.GCP_SERVICE_KEY }}
        run: |
          # 檢查 Key 是否存在
          if [ -z "$GCP_SERVICE_KEY" ]; then
            echo "❌ 錯誤：GitHub Secret 'GCP_SERVICE_KEY' 是空的！"
            echo "請去 GitHub Settings -> Secrets -> Actions 確認變數名稱是否正確。"
            exit 1
          fi
          
          # 寫入檔案
          echo "$GCP_SERVICE_KEY" > service_key.json
          
          # 檢查檔案是否有效 (不是空的)
          if [ ! -s service_key.json ]; then
            echo "❌ 錯誤：service_key.json 建立失敗，檔案是空的。"
            exit 1
          fi
          
          echo "✅ service_key.json 建立成功，準備執行 Python。"

      - name: Run Release Tracker
        run: |
          python stock_release_tracker.py
